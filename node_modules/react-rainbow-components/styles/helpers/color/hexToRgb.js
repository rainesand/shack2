"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = hexToRgb;

var _isHexColor = _interopRequireDefault(require("./isHexColor"));

function hexToRgb(color) {
  if ((0, _isHexColor["default"])(color)) {
    var hexColor = color.substr(1);
    var re = new RegExp(".{1,".concat(hexColor.length / 3, "}"), 'g');
    var regColors = hexColor.match(re);

    if (regColors) {
      var colors = regColors.map(function (n) {
        if (n.length === 1) {
          return n + n;
        }

        return n;
      });
      return "rgb(".concat(colors.map(function (n) {
        return parseInt(n, 16);
      }).join(', '), ")");
    }
  }

  return '';
}